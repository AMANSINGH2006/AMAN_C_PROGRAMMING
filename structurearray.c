#include <stdio.h>
#include<string.h>
int main()
{
 typedef struct man{
    char name[20];
    int age;
    float salary;
 }man;
   struct arr[3];
   printf("Enter the details\n");
   for( int i=0;i<3;i++){
   scanf("%[^\n]s %d %f",arr.name[i],&arr.age[i],&arr.salary[i]);}
   
   for(int i=0;i<3;i++){
   printf("%s %d %f\n",arr.name[i],arr.age[i],arr.salary[i]);}
   return 0;
   }